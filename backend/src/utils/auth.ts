// import jwt from 'jsonwebtoken';
// import { config } from '~/config';
//
// export const createToken = (
//   data: string | Buffer | any,
//   expiresIn = config.jwt.defaultExpiresIn,
// ): string => {
//   return jwt.sign(data, config.jwt.secret, { expiresIn });
// };
